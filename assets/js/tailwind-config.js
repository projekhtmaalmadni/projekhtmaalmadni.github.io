// Tailwind configuration replacement
// This replaces the inline tailwind.config that was dependent on CDN

// Since we're using a pre-built CSS file, we don't need the dynamic config
// The styles are already compiled into our local tailwind.css file

console.log('Local Tailwind CSS loaded successfully');